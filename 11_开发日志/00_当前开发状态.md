# 当前开发状态

更新时间：2026-07-11 08:35

> 说明：本文后半部分保留此前各阶段的过程记录。若旧记录与下面的「产品能力权威状态」或「当前打开项目状态」冲突，以这两节及最新专项日志为准。

## 当前产品目标

当前产品是「组件化 H5 Agent 生产工作台」的本地个人工具雏形，不是普通图片生成器、Figma 克隆、SaaS 后台或 Open Design 的完整复刻。

核心方向保持为：

```text
需求输入 -> 生成结构化 H5 原型 -> 人改原型 -> 生成长图 -> 导出
```

## 当前运行方式

- 项目路径：`/Users/yiming/Desktop/H5自动生图工作台`
- 代码包路径：`/Users/yiming/Desktop/H5自动生图工作台/10_产品代码包`
- 前端启动命令：`pnpm --filter @h5-workbench/web dev`
- daemon 启动命令：`pnpm --filter @h5-workbench/daemon dev`
- 同时启动命令：`pnpm dev`
- 本地访问地址：`http://127.0.0.1:5178/`
- 本地 daemon 地址：`http://127.0.0.1:7458/`
- 当前项目落盘目录：`10_产品代码包/.h5-workbench/projects/current/`
- 历史项目快照目录：`10_产品代码包/.h5-workbench/projects/snapshots/`
- 是否需要后端：Home 页面可独立打开；创建项目、保存、上传素材、Agent、候选应用、确认、长图 mock 和导出均要求 daemon 在线。状态变更失败时不再伪造前端成功结果。

## 产品能力权威状态

- 产品结构未改变：仍是「一级 Home + 二级 Project Workspace」，没有改成营销页、普通后台或自由画布。
- 正式生产事实源已收口：`prototype.html`、`h5-artifact.json`、候选 blueprint、确认版本和长图输入必须同源。
- 候选 Review & Apply 已升级为 HTML + blueprint 双哈希：查看候选时 daemon 同时返回 `candidateHtmlHash` / `blueprintHash`，Apply 缺任一哈希会拒绝；应用前重新计算，不一致返回 `409 candidate_changed`。正式 HTML 和 applied blueprint 使用已校验的同一份内存内容落盘，避免检查后再次读盘造成竞态。
- 素材已引入受管理元数据：`assetId`、`slotId`、相对路径、SHA-256、真实 MIME、字节数、原文件名、上传时间与验证状态。上传和确认均检查真实字节类型、路径边界、普通文件、字节数与哈希；旧素材只在安全复核后迁移，并在迁移写入前备份旧 Artifact。
- H5Artifact 已接入语义完整性校验：section id / sectionId / order、组件 registry、槽位引用、usedBySections、受管理素材引用、可见页面总高度与 legacy prototypeSource 均须一致；失败返回结构化 `artifact_integrity_failed`，不得写入正式文件。
- 确认原型现在生成 `production-integrity.json`，记录正式 Artifact、prototype、legacy blueprint 和必需素材哈希；`long_image_generation` 启动前重新读取磁盘并复核。确认后任一生产输入变化，长图 mock 返回 `409 production_integrity_failed`，不会启动 runner。
- mock 长图 runner 不再把正式 Artifact 改为 `generating/generated`；生产版本保持 `confirmed`，生成进度由 RunState / FileManifest 承担，避免运行过程自行破坏确认清单。
- 旧版本遗留数据已完成安全对账：历史 legacy 原型可对应 9 个 section，旧 4 模块 Artifact 已备份为 `h5-artifact.pre-reconcile.json`。active `current/` 一律以 daemon 和磁盘 `project.json` 为准；历史 legacy 项目只能作为 snapshot / candidate 参考，不能当作当前状态。
- 候选应用现在同时应用候选 HTML 和 blueprint 映射后的 H5Artifact；正式 legacy blueprint 固化到 `legacy-task/applied/legacy-blueprint.applied.json`，不再长期引用会被后续生成覆盖的 candidate blueprint。
- `prototype.dry-run.html` 只用于技术验证，不再允许应用为正式原型。
- KV 和素材已支持真实本地文件落盘；Home 启动 legacy 候选生成必须上传真实 KV，不再用文件名或占位图冒充已上传素材。
- 确认原型会检查必需素材的状态和磁盘文件；缺少必需 KV 时会正确阻止确认和长图生成。
- Inspector 的组件替换和主题风格修改已纳入 daemon patch 协议，不再只改浏览器内存；当正式原型来源为 `legacy_runner` 时，全局 Artifact 标题、组件替换、全局主题和任意 props 会被明确拒绝为 `requires_regeneration`，不会静默降级成简易模板。
- 原型编辑已新增 `PrototypeRendererAdapter`：`artifact_renderer` 继续用简易 renderer；`legacy_runner` 用 HTML parser 依据 `data-section` / `data-section-id` 和 `data-component` 做同源局部编辑，保留旧 HTML 的 CSS、布局、素材和模块结构。
- legacy 同源编辑会同时备份 `prototype.previous.html` 与 `h5-artifact.previous.json`，重新计算正式 HTML 的 SHA-256，写回 `prototypeSource.sourceHash`；确认原型会继续校验该哈希和 blueprint 哈希。
- 人工 Artifact patch 与素材保存已使用多文件暂存、提交和失败回滚；受控 I/O 故障测试证明 `prototype.html`、Artifact、manifest、project、备份和素材不会留下半套提交。素材 PUT / DELETE 会先等待 daemon 持久化队列清空。
- Web API client 会保留 daemon 的 HTTP status 和结构化 `error.code`；legacy 素材操作的前端禁用态与 artifact renderer 正常态均有服务端渲染测试，不再依赖字符串猜测错误类型。
- 多项目不再是浏览器假 tab：新建项目会轮转 current 到 snapshots，最近项目可从磁盘列表打开并恢复完整状态。
- daemon 状态损坏时不再静默重置项目；会保留原文件并写 recovery report。daemon 重启时会把中断 run 的残留 `running` 状态恢复为 `error`。
- 运行并发已收紧：已有 run 执行时，新 run、切换项目和替换当前项目返回 409，不再复用错误 runId 或交叉写目录。
- daemon 只允许本地工作台 Origin，runner 使用环境变量白名单，并在中止/超时时升级到 SIGKILL，避免继承全部敏感环境或残留子进程。
- 等待中的 `slice-plan.json`、`long-image.png`、`export.zip` 不会因磁盘旧文件而被 API 当作有效产物；导出只下载明确标识的 mock JSON 元数据，不伪装成 ZIP。
- 回归测试现覆盖 18.1 全部旧用例，以及双哈希缺失/变化/竞态、候选提交失败回滚、PNG/JPEG/WebP 真实字节、伪 MIME、JSON、目录、越界与 symlink、旧素材迁移备份、Artifact 语义错误、通用 patch 伪造素材状态、确认原子回滚、daemon 重启复核和四类生产输入篡改；`pnpm test` 最终真实执行 42 项测试（daemon 38、Web 4），全部通过。

## 当前打开项目状态（以 daemon 与 `current/project.json` 为准）

- 检查时间：2026-07-11 08:35 CST。
- `projectId`：`daemon_project_1783715912582`；项目名称为「游戏活动 H5」。本轮开始和结束均为同一 projectId，没有创建、打开、轮转或覆盖正式项目。
- 正式 Artifact：4 个 section、`prototypeSource.kind = artifact_renderer`、`status = draft`。
- 素材状态：`kv_main` 已上传，现有旧记录指向 `assets/kv_main.jpg`；其余 5 个 imageSlots 为空。该 current 的 KV 早于本轮 managed asset 协议，尚无 `assetId` 元数据；新 daemon 在下一次正式确认时只会在真实字节、路径、MIME、字节数和 SHA-256 全部通过后迁移，否则阻止确认。
- 候选状态：`prototype.candidate.html` 为 `ready_for_review`，尚未应用；正式 `prototype.html` 仍是 artifact renderer 草稿。
- run 状态：磁盘与当前 daemon API 仍显示 `running / prototype_review`。为遵守“不改正式 current”边界，本轮没有重启或恢复正式 7458 daemon，也没有把该状态擅自改为 error/done；新代码使用独立端口和临时数据目录完成 API 验收。
- 正式目录保护证据：本轮开始/结束 `current` 树哈希均为 `de2253901bd74dce9c33adfa988e3237c205e5b718b3b3e099cfa7eebd1acdb3`；`snapshots` 树哈希均为 `745070f63cdc7e52c9183d6acbe8c825d1f746e592f4305dd5046437cf7a728d`。
- 历史 9 模块 legacy 项目位于 snapshot：`10_产品代码包/.h5-workbench/projects/snapshots/daemon_project_1783649876109/1783701461779-033289aa-6b07-4022-bf28-23330a129236/`。它不是 active `current/`，只作为只读复制 fixture 验证同源编辑兼容性。

## 当前已完成

- 一级 Home：默认进入 Home，通过居中的需求 Composer 启动 H5 任务；保留模板、H5 类型、语言、设计规范、KV 入口和最近项目。
- 二级 Project Workspace：生成任务后进入项目工作区，保留顶部全局项目 tab 和右侧二级 tab。
- 多项目 tab：顶部项目 tab 支持切换、关闭和新建；项目列表与切换来自 daemon 的 `current/` + `snapshots/` 磁盘状态，不再只保存在浏览器内存。
- 左侧 Agent 面板：已改为产品化 Agent 执行面板，包含项目任务摘要、Run Checklist、Agent 输出、Produced Files、Activity / Log、下一步建议、底部输入框和可折叠 raw CLI log。
- 右侧设计文件：默认进入「设计文件」tab，固定展示 `brief.md`、`h5-artifact.json`、`prototype.html`、`critique.json`、`slice-plan.json`、`long-image.png`、`export.zip`。
- 原型预览：保留在「原型预览」tab 内；正式 legacy 原型用 iframe 展示真实 `prototype.html`，Inspector 使用同源正式 H5Artifact。
- 生成长图：已从前端 `setTimeout` mock 迁移为 daemon run API + SSE 驱动；右侧流程仍展示确认原型、生成切片计划、分段生图、拼接长图、质量检查、导出长图六步状态。
- 素材/KV：artifact renderer 原型已接入 daemon 本地上传、替换、清除；操作会更新正式 H5Artifact，已确认原型会回到 edited。legacy runner 正式原型的素材操作会在落盘前返回 `409 requires_regeneration`，前端会禁用对应操作并提示重新生成候选原型。
- mock daemon 接口层：已提供当前项目、文件清单、Artifact、RunState、素材槽位、AgentEvent JSON 和 AgentEvent SSE 流接口。
- 前端 API client：已新增 daemonClient；所有状态变更以 daemon 结果为准。daemon 不可用时明确报错，不再把浏览器 fallback 状态伪装成已创建、已生成或已导出。
- daemon run：已新增 `POST /api/runs/current/start`，可启动 `long_image_generation` mock run，并通过 SSE 推送 AgentEvent、RunState、FileManifest 和 project state。
- 项目文件落盘：daemon 启动时会初始化或读取 `10_产品代码包/.h5-workbench/projects/current/`，并维护 `project.json`、`brief.md`、`h5-artifact.json`、`prototype.html`、`critique.json`、`slice-plan.json`、`files.manifest.json`、`run-state.json`、`events.jsonl` 和 `output/` 占位产物。
- run history 持久化：`AgentEvent` 已追加写入 `events.jsonl`，`RunState` 和 `FileManifest` 在 mock run 过程中同步写入磁盘；daemon 重启后能恢复上一轮状态。
- Runner Adapter 抽象层：daemon 已新增 `runners/` 模块，`long_image_generation` mock 流程已从 `server.ts` 迁移到 `mockLongImageRunner`，server 只负责选择 runner、应用 RunnerEvent、写入 projectStore 和推送 SSE。
- Codex CLI 最小 POC：已新增 `codex_cli_probe` runType，可由 daemon runner 调用本机 Codex CLI，读取 `current/brief.md`，生成 `current/codex-probe-result.md`，并通过 AgentEvent / SSE 反馈结果。
- Codex Artifact 草稿：已新增 `codex_artifact_draft` runType，可由 Codex CLI 根据 `current/brief.md` 和 H5Artifact 类型说明生成 `current/h5-artifact.draft.json`，不覆盖正式 `h5-artifact.json`。
- Agent 输入框接 daemon：二级项目页左侧输入框已接入 `POST /api/agent/messages`，默认以 `probe_message` intent 触发 Codex probe runner，生成 `agent-message-result.md`，并通过 SSE 回显用户消息、Codex 建议和 produced file。
- Artifact Draft Review & Apply：已新增人工审核/应用草稿闭环，用户可在「设计文件」查看 `h5-artifact.draft.json`，手动应用到正式 `h5-artifact.json` 或放弃草稿；应用前会校验 JSON 和 sections / imageSlots 基础字段，并备份上一版为 `h5-artifact.previous.json`。
- Artifact 应用后刷新链路：应用草稿后会重新生成 `prototype.html`，刷新前端当前项目 state，原型预览、Inspector、素材/KV 槽位和设计文件状态会跟随最新正式 `h5-artifact.json` 更新。
- 基础人工编辑能力：原型预览 tab 内的 Inspector 已支持编辑 artifact title、当前 section 文案、基础 string/number props、section height、上移/下移、隐藏/恢复模块，并通过 daemon `PATCH /api/projects/current/artifact` 保存回正式 `h5-artifact.json`。
- 原型编辑同源渲染稳定化：新增 `apps/daemon/src/prototype-renderer/`。legacy 原型保存时不再切换 `prototypeSource` 到 `artifact_renderer`，支持安全的基础文案、模块高度、隐藏/恢复和同父容器排序；保存会保留 `legacy_applied` manifest 状态并更新 HTML 哈希。旧 runner HTML 无法可靠唯一定位的文案字段、全局 Artifact 标题、组件替换、全局主题、任意 Props 和素材槽位映射返回 `requires_regeneration`，正式 HTML / Artifact 不变。Inspector 对 legacy 来源仅补充必要提示并禁用这些不支持的控件。
- 端到端链路稳定化：Home / Project / daemon 状态同步已加强，Inspector 保存前会强制拉取 `/api/projects/current` 最新 Artifact，遇到旧 `selectedSectionId` 会自动同步并中止保存。
- 确认原型闭环：已新增 `POST /api/projects/current/artifact/confirm`，确认后正式 `h5-artifact.json` 写入 `confirmedAt` 和 `confirmedVersionId`，`prototype_review` 阶段置为 `done`，并重新生成 `prototype.html`。
- 长图生成前置门槛：`long_image_generation` mock run 已要求正式 Artifact 状态必须是 `confirmed`；未确认时 daemon 返回 409、RunState 进入 `blocked`，前端提示「请先确认原型」且不会降级成本地 mock。
- mock 导出闭环：确认后可启动长图 mock run，`slice-plan.json` 会包含 `confirmedVersionId`，`output/long-image.png` 和 `output/export.zip` 仍是明确标注的 JSON placeholder，不伪装成真实图片或 ZIP。
- 旧项目能力审计：已完成旧资料项目和其指向的旧代码项目审计，结论是存在可复用的 Python 分层 pipeline、执行版 Skill 文档和知识库，但没有可直接安装运行的标准 Codex `SKILL.md`；本轮未接入旧能力。
- 旧 runner Adapter 最小落地：已新增 `legacy_adapter_probe` runType、`legacy-adapter/` 契约和 mock adapter，可把当前工作台项目映射到 `legacy-task/` 输入输出目录；本轮只验证 adapter 映射，不真实运行旧 Python pipeline，不覆盖正式 `prototype.html` 和 `h5-artifact.json`。
- 旧 runner dry-run 可行性验证：已新增 `legacy_runner_dry_run` runType，可由 daemon 调用旧项目 `.venv/bin/python -m h5_agent.runner.run_local_prototype`，输出限制在 `legacy-task/dry-run/`；本轮真实执行了原型 runner dry-run，exitCode 为 0，并生成 `prototype.dry-run.html`、`03_prototype_full.png`、切片和质检报告，但没有运行 `run_local_h5.py`，没有接真实生图，也没有覆盖正式文件。
- 旧 runner 原型 Review & Apply：产品候选 `prototype.candidate.html` 支持人工查看、应用、放弃；应用时同时把候选 blueprint 映射到正式 H5Artifact，并备份旧 HTML / Artifact。技术 `prototype.dry-run.html` 只允许查看和放弃，不允许进入正式主线。
- Home 接 legacy 原型候选生成：Home 会把真实 KV 文件写入当前项目后再启动 `legacy_prototype_generation`；不上传 KV 时返回 422。候选仍需人工 Review & Apply，不会自动覆盖正式文件。

## 当前主要组件

- `10_产品代码包/packages/agent-contract/src/index.ts`：AgentEvent、FileManifest、RunState、RunType 等前后端共享协议；新增 `PrototypeCandidateReview`、候选双哈希 Apply 请求、`ProductionIntegrityRecord` 和 `production_integrity` 文件类型。通用 `update_image_slot` 仅允许清空/标记缺失，不能伪造 uploaded / locked / generated。
- `10_产品代码包/packages/h5-artifact/src/index.ts`：H5Artifact、H5Section、ImageSlot、组件 registry 和 sampleArtifact；新增 managed asset 元数据、`ImageSlot.assetId` 和 `validateH5ArtifactIntegrity()` 语义校验。
- `10_产品代码包/apps/daemon/src/mockProjectState.ts`：daemon 侧统一 mockProjectState 生成器，用同一协议返回当前项目状态。
- `10_产品代码包/apps/daemon/src/projectStore.ts`：当前项目本地文件存储层；新增候选双哈希 Review/Apply、同一内存内容提交、applied blueprint、真实素材字节检测、旧素材安全迁移、Artifact 语义校验、`production-integrity.json` 生成/复核和多文件事务回滚。
- `10_产品代码包/apps/daemon/src/prototype-renderer/`：原型渲染策略层。`artifactRenderer.ts` 负责现有简易 Artifact HTML；`legacyRunnerRenderer.ts` 使用 Cheerio parser 保留旧 runner DOM，仅在候选节点恰好一个时修改基础文案，并以归一化后的 Artifact 高度写入属性与 CSS；`index.ts` 按 `prototypeSource.kind` 选择策略。
- `10_产品代码包/apps/daemon/src/runners/types.ts`：Runner、RunnerContext、RunnerEvent、RunnerHandle 等 daemon runner 抽象。
- `10_产品代码包/apps/daemon/src/runners/mockLongImageRunner.ts`：当前 `long_image_generation` mock 长图流程；启动必须携带已复核的 ProductionIntegrityRecord，且不再改写正式 Artifact status。
- `10_产品代码包/apps/daemon/src/runners/index.ts`：runner 注册和选择入口。
- `10_产品代码包/apps/daemon/src/runners/codexCliRunner.ts`：Codex CLI runner，支持 `codex_cli_probe`、左侧 Agent message probe 和 `codex_artifact_draft`；Artifact draft 会生成 `h5-artifact.draft.json` 并做 JSON 校验。
- `10_产品代码包/apps/daemon/src/runners/legacyAdapterProbeRunner.ts`：旧 runner adapter probe runner，只准备 `legacy-task/` 输入输出映射和 mock 产物，不执行旧 pipeline。
- `10_产品代码包/apps/daemon/src/runners/legacyRunnerDryRun.ts`：旧 runner dry-run runner，发现旧 runner 入口，准备 dry-run 兼容输入，执行 `run_local_prototype.py`，写入 stdout/stderr/exit-code/report，并将 dry-run report / dry-run prototype 注册进 FileManifest。
- `10_产品代码包/apps/daemon/src/runners/legacyPrototypeGenerationRunner.ts`：产品流程用旧 runner 原型候选生成 runner，从 Home brief 触发，执行 `run_local_prototype.py`，输出限制在 `legacy-task/candidate/`，并映射 `prototype.candidate.html`、候选预览图、blueprint 和质量报告。
- `10_产品代码包/apps/daemon/src/legacy-adapter/types.ts`：定义 `LegacyTaskInput`、`LegacyTaskConfig`、`LegacyTaskPaths`、`LegacyOutputManifest`、`LegacyAdapterResult` 和 adapter 状态。
- `10_产品代码包/apps/daemon/src/legacy-adapter/legacyTaskMapper.ts`：把当前 projectState 写成旧 runner 未来可读取的 `legacy-task/input/` 与 `legacy-task/config/task.json`。
- `10_产品代码包/apps/daemon/src/legacy-adapter/legacyOutputMapper.ts`：把旧 runner 未来输出映射回当前 FileManifest；当前新增 `prototype.generated.html` 的 adapter mock 文件项。
- `10_产品代码包/apps/daemon/src/legacy-adapter/mockLegacyAdapter.ts`：当前 mock adapter，写入 `legacy-task/output/prototype.generated.html` 和 `legacy-output-manifest.json`，明确标注不是旧 pipeline 真实输出。
- `10_产品代码包/apps/daemon/src/server.ts`：本地 API 服务；新增 `GET /api/prototypes/candidate/review`、双哈希候选 Apply、结构化完整性错误，并在 `long_image_generation` 前复核 production-integrity。仍只启动 mock 长图 runner。
- `10_产品代码包/apps/web/src/api.ts`：统一 API 错误类型，保留 HTTP status、daemon `error.code` 和详细响应。
- `10_产品代码包/apps/web/src/api/daemonClient.ts`：前端访问 daemon 的 API client；候选查看现在取得双哈希，Apply 同时提交两个 expected hash。
- `10_产品代码包/apps/web/src/data/mockProjectState.ts`：统一 mockProjectState，驱动 Agent 面板、设计文件、原型预览、长图流程和素材槽位。
- `10_产品代码包/apps/web/src/App.tsx`：主状态、项目创建、协议 mock 数据接入、daemon run 启动、Agent message 发送、SSE 状态同步和本地 fallback；Home「生成 H5 原型」已优先调用 `createProjectFromBrief()` 启动 `legacy_prototype_generation`；Inspector 保存前会重新拉取 daemon 最新 state，确认原型走 daemon confirm API。
- `10_产品代码包/apps/web/src/components/WorkspaceChrome.tsx`：顶部全局 tab、主页 tab、项目 tab、关闭和新建入口。
- `10_产品代码包/apps/web/src/components/HomeEntry.tsx`：一级 Home 需求 Composer、模板入口、最近项目。
- `10_产品代码包/apps/web/src/components/ProjectShell.tsx`：二级项目工作台外壳，左侧 Agent 区和右侧工作区布局。
- `10_产品代码包/apps/web/src/components/AgentRunPanel.tsx`：产品化 Agent 执行面板；底部输入框已能提交消息并显示 sending / error 状态。
- `10_产品代码包/apps/web/src/components/ProjectWorkspace.tsx`：右侧二级 tab 内容分发。
- `10_产品代码包/apps/web/src/components/DesignFilesView.tsx`：生产链路文件产物区；已包含 Artifact Draft Review 区和旧 runner 候选原型 Review 区，可查看、应用或放弃 `h5-artifact.draft.json`、`prototype.candidate.html` 与兼容的 `prototype.dry-run.html`。
- `10_产品代码包/apps/web/src/components/LongImageView.tsx`：mock 长图生成流程。
- `10_产品代码包/apps/web/src/components/AssetSlotsView.tsx`：素材/KV 槽位管理；legacy 正式原型会禁用上传、替换和清除并提示重新生成候选原型。
- `10_产品代码包/apps/web/src/components/ArtifactViewer.tsx`：H5 原型预览；当正式 `prototype.html` 已由旧 runner 应用时，可用 iframe 预览正式 HTML，而不是继续只看 Artifact 简易卡片渲染。
- `10_产品代码包/apps/web/src/components/InspectorPanel.tsx`：原型模块属性编辑；当前支持最小人工编辑和保存修改入口。
- `10_产品代码包/apps/web/src/styles.css`：Open Design 风格的工作台视觉层。

## 当前 mock 数据

- `packages/h5-artifact/src/index.ts`：`sampleArtifact`、H5Artifact 类型、ImageSlot 类型、组件 registry。
- `packages/agent-contract/src/index.ts`：`AgentEvent`、`FileManifest`、`RunState`、RunStage 等协议类型。
- `apps/web/src/data/mockProjectState.ts`：统一 mockProjectState、mock AgentEvent、mock FileManifest、mock RunState、mock ImageSlot。
- `apps/daemon/src/mockProjectState.ts`：daemon 返回给前端的当前 mockProjectState。
- 目前模拟了：需求生成项目、sampleArtifact 原型载入、Agent 事件输出、AgentEvent SSE 流、RunState SSE 流、FileManifest SSE 流、长图流程状态变化、文件产物状态变化、素材槽位管理。

## 尚未接入

- Codex CLI：已完成最小 POC、Artifact 草稿生成和左侧 Agent 输入 probe，可通过 daemon 调用；尚未接正式原型生成、编辑采用或生图链路。
- Claude CLI：未接入；同样应位于 daemon 层。
- 后端 daemon：已完成 mock API 层、long image mock run、当前项目文件落盘、run history 持久化和 Runner Adapter 抽象层；尚未接真实 CLI、真实 runner 和真实事件来源。
- 真实素材上传：artifact renderer 原型已支持 6 类 ImageSlot 的本地文件写入、替换、清除和 Artifact 映射；legacy runner 正式原型的三类素材操作必须重新生成候选原型，尚未做素材库、缩略图处理、批量上传与云存储。
- 真实长图生成：未调用旧 Python runner，只模拟切片、分段生图和拼接流程；mock 长图必须先确认原型。
- 旧 Python runner：已完成 adapter 契约、`legacy_adapter_probe` mock 验证、`legacy_runner_dry_run` 可行性验证、旧 runner 原型候选 Review & Apply，以及 Home 入口触发 `legacy_prototype_generation`。`run_local_prototype.py` 已能由 daemon 从 Home 正常生产流程调起，输出 `legacy-task/candidate/prototype.candidate.html` 并可手动应用为正式 `prototype.html`。尚未运行 `run_local_h5.py`，也未接真实生图。
- 导出：未生成真实 PNG/WebP/ZIP；`long-image.png` 和 `export.zip` 当前是 `output/` 下的 JSON placeholder 文件，内容明确标注 `placeholder: true` 和 `mock: true`，后续由真实 runner 产物替换。
- 高质量原型生成：Home 已能触发旧 `run_local_prototype.py` 生成 `prototype.candidate.html`，人工 Apply 时会把旧 runner blueprint 映射回正式 H5Artifact；当前映射是受控近似转换，仍不是完整组件语义还原，也没有进入真实长图生图。

## 历史过程记录（非当前状态）

以下内容保留每一步的验收事实，便于回溯；其中出现的「当前项目」「当前 KV」「模块数量」只描述当时的测试项目，不能覆盖本文件前部由 daemon 与磁盘确认的 active `current/` 状态。

## Guardian 检查结论

本轮「工作台结构阶段验收」结论：当前前端能正常运行，结构没有跑偏。

- 保持了「一级 Home + 二级 Project Workspace」结构。
- Home 仍负责启动任务，没有变成后台表单或营销页。
- Project Workspace 默认是设计文件和 Agent 执行区，没有把大编辑器作为唯一主体。
- 左侧 Agent 区是产品化事件面板，raw CLI 只作为调试折叠区。
- 右侧覆盖设计文件、原型预览、长图生成、素材/KV 四类工作区。
- 长图生成和素材/KV 都是第一阶段可接后端的 mock 结构，没有提前复活旧 Dify / ComfyUI 链路。

## 结构阶段验收结果

- 页面运行：`http://127.0.0.1:5178/` 可访问，HTTP 返回 `200 OK`。
- 命令验证：`pnpm typecheck` 通过；`pnpm --filter @h5-workbench/web build` 通过。
- 默认入口：刷新后默认进入 Home，显示「你想生成什么 H5？」和需求 Composer。
- 一级 Home：存在中央 Composer、5 个模板卡片、最近项目区域、顶部全局 tab。
- 二级 Project Workspace：点击「生成 H5 原型」后进入项目页，顶部显示「主页」和短项目 tab「游戏活动 H5」。
- 左侧 Agent 面板：存在项目任务摘要、Agent 输出、Produced Files、Activity / Log、下一步建议、底部继续输入框和 raw CLI log 折叠区。
- 设计文件：默认 tab 为「设计文件」，存在 `brief.md`、`h5-artifact.json`、`prototype.html`、`critique.json`、`slice-plan.json`、`long-image.png`、`export.zip`。
- 原型预览：存在 H5 Artifact viewer、Inspector、4 个可选模块，模块选中态正常。
- 生成长图：存在确认原型、生成切片计划、分段生图、拼接长图、质量检查、导出长图六步流程，状态为 `waiting`。
- 素材/KV：存在 6 个素材槽位，并显示上传/清除入口和 imageSlots 映射。
- 控制台：刷新后未发现新的 console error。

## 数据协议层状态

本轮已完成前端未来接 daemon / Codex CLI / 生图服务所需的第一版协议层落地。

- AgentEvent：已迁移到 `packages/agent-contract`，覆盖 `run_started`、`assistant_message`、`checklist_update`、`file_written`、`artifact_updated`、`next_action`、`run_finished`、`error`、`raw_log`。
- H5Artifact：已在 `packages/h5-artifact` 扩展 `artifactId`、`title`、`h5Type`、`designSystem`、`sections` 协议字段、全局 `imageSlots`。
- FileManifest：已在 `packages/agent-contract` 定义，并由 `mockProjectState` 生成右侧 7 个核心产物。
- RunState：已在 `packages/agent-contract` 定义 8 个阶段：intake、prototype_generation、prototype_review、slice_planning、image_generation、stitching、quality_check、export。
- ImageSlot：已在 `packages/h5-artifact` 定义 6 个产品槽位，包括 KV 主视觉、奖励素材、商品/道具图、品牌 Logo、参考风格图、活动规则/文案资料。
- mockProjectState：已统一驱动左侧 Agent 面板、右侧设计文件、原型预览、生成长图流程、素材/KV 槽位。

## mock daemon 接口层状态

本轮已完成第三步：本地 mock daemon/API 层。

- `GET /api/health`：返回 daemon 状态、mock 模式和当前项目 ID。
- `GET /api/projects/current`：返回统一 `mockProjectState`。
- `POST /api/projects/current`：根据 Home 输入的 brief 和 config 创建当前 mock 项目。
- `GET /api/projects/current/files`：返回 `FileManifest`。
- `GET /api/projects/current/artifact`：返回 `H5Artifact`。
- `GET /api/projects/current/run-state`：返回 `RunState`。
- `GET /api/projects/current/assets`：返回 `ImageSlot[]`。
- `GET /api/runs/current/events`：返回 `AgentEvent[]` JSON。
- `GET /api/runs/current/events/stream`：返回 SSE mock AgentEvent 流。
- 前端 `daemonClient` 已封装上述接口；Home 生成任务优先走 daemon，失败时回退本地 mock。

## daemon run / SSE 长图状态

本轮已完成第四步：把「生成长图」从前端 `setTimeout` mock 迁移到 daemon run API + SSE。

- `POST /api/runs/current/start`：启动 `long_image_generation` mock run。
- daemon 内部维护运行中 `RunState`，逐步推进 `prototype_review`、`slice_planning`、`image_generation`、`stitching`、`quality_check`、`export`。
- daemon 内部维护运行中 `FileManifest`，逐步把 `slice-plan.json`、`long-image.png`、`critique.json`、`export.zip` 更新为 `done`。
- `/api/runs/current/events/stream` 现在不仅推送 `agent_event`，也推送 `run_state`、`file_manifest`、`project_state` 和 `done`。
- 前端点击「生成长图」会调用 daemon `startRun("long_image_generation")`，再通过 SSE 同步左侧 Agent 面板、右侧长图流程和设计文件状态。
- daemon 不可用时，前端仍降级到本地 fallback，并在左侧 Agent 面板显示「daemon 未连接，当前降级为前端本地 mock」。

## 项目文件落盘与 run history 持久化状态

本轮已完成第五步：让 daemon 的当前项目状态从纯内存 mock 迁移到本地项目目录。

- 本地运行数据目录：`10_产品代码包/.h5-workbench/projects/current/`。
- `.h5-workbench/` 已在 `10_产品代码包/.gitignore` 中忽略，不作为源码提交内容。
- daemon 启动时会先读取 `current/project.json`、`h5-artifact.json`、`files.manifest.json`、`run-state.json` 和 `events.jsonl`；如果目录不存在，则用 `mockProjectState` 初始化。
- 当前已落盘文件：`project.json`、`brief.md`、`h5-artifact.json`、`prototype.html`、`critique.json`、`slice-plan.json`、`files.manifest.json`、`run-state.json`、`events.jsonl`。
- 当前输出占位：`output/long-image.png`、`output/export.zip`，内容是 JSON placeholder，不是真实图片或 ZIP。
- 新增文件内容 API：`GET /api/projects/current/files/:fileId`，可返回 `brief.md`、`h5-artifact.json`、`prototype.html`、`critique.json`、`slice-plan.json`、`long-image.png`、`export.zip` 的内容和 contentType。
- mock run 过程中会串行写入事件和状态，避免 run history 落盘顺序错乱。
- 验证结果：重启 daemon 后，`/api/projects/current` 能恢复上一轮 `generated` / `done` 状态和历史事件。

## Runner Adapter 抽象层状态

本轮已完成第六步：为未来接 Codex CLI / 旧 Python runner 抽出 daemon runner 层。

- 新增目录：`10_产品代码包/apps/daemon/src/runners/`。
- `types.ts` 定义 `Runner`、`RunnerContext`、`RunnerEvent`、`RunnerHandle`。
- `mockLongImageRunner.ts` 承接原本写在 `server.ts` 里的 mock 长图流程，继续逐步产出 `AgentEvent`、`RunState`、`FileManifest` 和完成信号。
- `index.ts` 提供 runner 注册和 `getRunner(runType)`。
- `codexCliRunner.ts` 只是未来 Codex CLI runner 的 skeleton，不会 spawn Codex，不要求 key，也不会执行真实命令。
- `server.ts` 现在只负责接收 `POST /api/runs/current/start`、选择 runner、应用 RunnerEvent、写入 projectStore、广播 SSE。
- 验证结果：`POST /api/runs/current/start` 能通过 `mockLongImageRunner` 跑完整长图 mock run；SSE、落盘和重启恢复保持正常。

## Codex CLI 最小 POC 状态

本轮已完成第七步：验证 daemon 可以通过 runner 调用本机 Codex CLI，并把结果写回当前项目目录。

- 本机 `codex` 命令检查通过：`/opt/homebrew/bin/codex`，版本 `codex-cli 0.143.0`。
- 新增 runType：`codex_cli_probe`。
- 启动方式：`POST /api/runs/current/start`，请求体 `{ "runType": "codex_cli_probe" }`。
- 执行目录限制：`10_产品代码包/.h5-workbench/projects/current/`，runner 的 `cwd` 和 `codex exec --cd` 都指向该目录。
- Codex CLI 调用方式：`codex exec --cd <current> --sandbox read-only --skip-git-repo-check --ephemeral --output-last-message <current>/codex-probe-result.md -`。
- POC 输入：daemon 读取 `current/brief.md`，并把 brief 内容放入最小 prompt。
- POC 输出：`current/codex-probe-result.md`，包含一句话理解、建议 H5 类型和建议理由。
- 本轮没有修改 `h5-artifact.json`，没有生成 `prototype.html`，没有接真实生图。
- SSE 验证：可以看到 `run_started`、`assistant_message`、`file_written: codex-probe-result.md`、`run_finished`；失败时会产生清晰 `error` event。
- 兼容验证：`long_image_generation` mock runner 仍能完整跑完，核心文件状态仍会变为 `done`。

## Codex 生成 Artifact 草稿状态

本轮已完成第八步：让 Codex CLI 根据当前 brief 生成一个结构化 H5Artifact 草稿文件。

- 新增 runType：`codex_artifact_draft`。
- 启动方式：`POST /api/runs/current/start`，请求体 `{ "runType": "codex_artifact_draft" }`。
- 输入：`current/brief.md`，以及 daemon 从 `packages/h5-artifact/src/index.ts` 中读取的 H5Artifact 类型说明节选。
- 输出：`current/h5-artifact.draft.json`。
- 失败 raw 输出：`current/codex-artifact-draft.raw.md`。
- 正式文件保护：不会覆盖 `current/h5-artifact.json`。
- JSON 校验：daemon 会 parse draft，并检查顶层 H5Artifact 字段、4-6 个 sections、3-6 个 imageSlots、section 必需字段。
- FileManifest：成功后会新增 `h5-artifact.draft.json` 文件项，kind 为 `artifact_draft`，status 为 `draft`。
- API：`GET /api/projects/current/files/file_artifact_draft` 可读取草稿内容。
- 本轮实际生成结果：`title = Game Fiesta Premiada`，`sections = 5`，`imageSlots = 5`，合法 JSON。
- 兼容验证：`codex_cli_probe` 和 `long_image_generation` 仍可正常运行。

## Agent 输入框接 daemon 状态

本轮已完成第九步：二级项目页左侧 Agent 输入框不再是静态控件，已能驱动 daemon runner。

- 新增接口：`POST /api/agent/messages`。
- 请求体：`{ "message": "...", "intent": "probe_message" }`。
- 当前默认 intent：`probe_message`，映射到 `codex_cli_probe` runner。
- runner 输入：读取 `current/brief.md`，并结合用户输入 message 调用 Codex CLI。
- runner 输出：`current/agent-message-result.md`。
- FileManifest：新增 `file_agent_message_result`，filename 为 `agent-message-result.md`，kind 为 `agent_note`，status 为 `ready`。
- AgentEvent：新增 `user_message`，同时继续推送 `run_started`、`assistant_message`、`raw_log`、`file_written`、`run_finished` 或 `error`。
- 前端：`AgentRunPanel` 底部输入框已接 `sendAgentMessage()`；发送后会立即显示用户消息，按钮进入 sending 状态，SSE 回来后显示 Codex 建议和 produced file。
- fallback：daemon 不可用时左侧会明确显示「daemon 未连接，当前无法发送给 Codex」，不会静默无反应。
- 正式文件保护：本轮不会修改正式 `h5-artifact.json`，不会修改 `prototype.html`，不会接真实生图。
- 浏览器验证：在 `http://127.0.0.1:5178/` 从 Home 进入项目页后，输入「把奖励模块往前放，整体更有活动氛围」可以看到用户消息、Codex 建议、`agent-message-result.md` 和 run finished。

## Artifact Draft Review & Apply 状态

本轮已完成第十步：Codex 生成的 `h5-artifact.draft.json` 不会自动覆盖正式 `h5-artifact.json`，必须由用户在前端手动查看并应用。

- 新增 API：`POST /api/artifacts/draft/apply`。
- 新增可选 API：`POST /api/artifacts/draft/discard`。
- 安全边界：只有当前项目 `FileManifest` 中存在且未 discarded 的 `file_artifact_draft` 才允许 apply / discard，避免误应用历史残留草稿。
- apply 前校验：daemon 会 parse JSON，并校验 H5Artifact 顶层字段、sections 基础字段和 imageSlots 基础字段。
- apply 成功后：
  - 正式 `h5-artifact.json` 被更新为已人工确认的草稿内容。
  - 草稿 artifact 状态规范为 `edited`。
  - 上一版正式 artifact 自动备份为 `h5-artifact.previous.json`。
  - `prototype.html` 按新 artifact 重新生成。
  - FileManifest 更新 `h5-artifact.json`、`prototype.html`、`h5-artifact.draft.json`、`h5-artifact.previous.json`。
  - AgentEvent 写入 `assistant_message`、`file_written` 和 `artifact_updated`。
  - 不会自动启动长图生成。
- discard 成功后：
  - `h5-artifact.draft.json` 在 FileManifest 中标记为 `discarded`。
  - 正式 `h5-artifact.json` 不变。
- 前端：右侧「设计文件」tab 中出现 Artifact Draft Review 区，提供「查看草稿」「应用草稿」「放弃草稿」。
- 本轮验证结果：当前正式 `h5-artifact.json` 已更新为 5 sections，状态 `edited`；`h5-artifact.previous.json` 存在；`GET /api/projects/current/artifact` 返回新 artifact；事件流包含 `artifact_updated`。

## Artifact 应用后原型预览刷新状态

本轮已完成第十一步：补齐 `h5-artifact.draft.json` 应用后的正式 artifact、prototype、前端预览和素材槽位同步链路。

- daemon apply 后会把草稿写入正式 `h5-artifact.json`，并基于最新正式 artifact 重新生成 `prototype.html`。
- `POST /api/artifacts/draft/apply` 成功后会更新 `FileManifest` 中的 `h5-artifact.json`、`prototype.html`、`h5-artifact.draft.json` 和 `h5-artifact.previous.json` 的状态与 `updatedAt`。
- AgentEvent 已补齐 `file_written: prototype.html` 和 `artifact_updated`，左侧 Agent 面板能看到原型文件重新生成。
- `projectStore` 增加草稿状态保护：只有 status 为 `draft` 的 `h5-artifact.draft.json` 可以被应用或放弃，已应用的草稿不会被重复应用。
- 前端应用草稿成功后会重新拉取 `/api/projects/current`，清理旧的 `selectedSectionId`，默认选中新 artifact 的第一个 section，并切到「原型预览」tab。
- 原型预览标题已优先显示 `artifact.title`，避免正式 artifact 更新后仍显示旧项目短标题造成误判。
- `AssetSlotsView` 的数据来源跟随最新 `project.artifact.imageSlots`，素材/KV 页面不再使用旧 mock 槽位。
- 设计文件页中已应用的 draft 会显示为 `done`，应用/放弃按钮禁用，提示需要重新生成新的 draft 后再操作。
- 本轮实际验收的正式 artifact：`title = Campanha de Recompensas do Jogo`，`sections = 5`，`imageSlots = 5`。
- API 验证：`GET /api/projects/current/artifact` 返回最新正式 artifact；`GET /api/projects/current/files/file_prototype` 返回包含最新标题和 5 个 section 的 `prototype.html`。
- 浏览器验收：在 `http://127.0.0.1:5178/` 点击「应用草稿」后无需手动刷新，右侧自动切到「原型预览」，标题、5 个模块、Inspector 当前 section 字段和素材/KV 5 个槽位均已同步。
- 正式备份：`10_产品代码包/.h5-workbench/projects/current/h5-artifact.previous.json` 存在。

## 基础人工编辑能力状态

本轮已完成第十二步：设计师可以在「原型预览」tab 内做最小人工编辑，并保存回正式 `h5-artifact.json`。

- 新增 API：`PATCH /api/projects/current/artifact`。
- 第一版 patch 操作：`update_artifact_title`、`update_section_text`、`update_section_prop`、`update_section_height`、`move_section_up`、`move_section_down`、`hide_section`、`restore_section`。
- 保存前会校验 section 存在、字段名合法、至少保留一个可见 section。
- 保存前会备份上一版正式 artifact 到 `h5-artifact.previous.json`。
- 保存后会更新正式 `h5-artifact.json`，重新生成 `prototype.html`，更新 FileManifest 的 `h5-artifact.json`、`prototype.html`、`h5-artifact.previous.json` 状态和 `updatedAt`。
- 保存后写入 AgentEvent：`assistant_message`、`file_written: h5-artifact.json`、`file_written: h5-artifact.previous.json`、`file_written: prototype.html`、`artifact_updated`。
- 前端 Inspector 支持编辑 artifact title、当前模块 textFields、基础 string/number props、模块高度、上移/下移、隐藏/恢复模块。
- 前端保存成功后会重新拉取 daemon 当前项目状态，原型预览无需手动刷新即可看到新文案。
- 当前验收 artifact：`title = Campanha Manual UI Save`，`sections = 5`，`visible sections = 5`，第一模块文案为 `Recompensas editadas no Inspector`。
- 当前仍未做完整 Figma、自由画布、真实生图和自动长图生成。

## 旧 runner Adapter 设计与最小落地状态

本轮已完成第十四步：在不运行旧 Python pipeline 的前提下，最小落地当前工作台与旧 runner / pipeline 之间的 adapter 协议。

- 新增 runType：`legacy_adapter_probe`。
- 启动方式：`POST /api/runs/current/start`，请求体 `{ "runType": "legacy_adapter_probe" }`。
- 新增目录：`10_产品代码包/apps/daemon/src/legacy-adapter/`。
- adapter 输入目录：`10_产品代码包/.h5-workbench/projects/current/legacy-task/input/`。
- adapter 配置目录：`10_产品代码包/.h5-workbench/projects/current/legacy-task/config/`。
- adapter 输出目录：`10_产品代码包/.h5-workbench/projects/current/legacy-task/output/`。
- 当前写入输入文件：`brief.md`、`h5-artifact.json`、`design-system.json`、`image-slots.json`、`assets-manifest.json`。
- 当前写入配置文件：`config/task.json`，其中明确 `mode = mock_prepare_only` 且 `willExecuteInThisRun = false`。
- 当前写入 mock 输出：`prototype.generated.html` 和 `legacy-output-manifest.json`。
- FileManifest 新增 `file_prototype_generated`，filename 为 `prototype.generated.html`，kind 为 `prototype_generated`，status 为 `adapter_mock`，描述明确写明「旧 runner adapter mock 产物，不是旧 pipeline 真实输出」。
- `GET /api/projects/current/files/file_prototype_generated` 可读取 mock HTML，contentType 为 `text/html; charset=utf-8`。
- RunnerEvent 已支持 `writeProjectFiles?: boolean`，`legacy_adapter_probe` 更新 FileManifest 时不会重写正式项目核心文件。
- 验证结果：`legacy_adapter_probe` 运行后，正式 `prototype.html` 和 `h5-artifact.json` 的 SHA-256 校验和保持不变。
- 本轮没有运行 `run_local_prototype.py`、`run_local_h5.py`，没有调用真实生图服务，也没有把旧项目文件复制进当前代码包。

## 旧 runner dry-run 可行性验证状态

本轮已完成第十五步：验证当前 daemon 是否能以受控方式调起旧项目原型 runner，并把结果限制在当前项目 dry-run 目录。

- 新增 runType：`legacy_runner_dry_run`。
- 启动方式：`POST /api/runs/current/start`，请求体 `{ "runType": "legacy_runner_dry_run" }`。
- 旧 runner 代码项目：`/Users/yiming/Documents/Codex/2026-06-17/h5-kv-h5-demo-dify-llm`。
- 旧资料项目：`/Users/yiming/Desktop/h5自动生图项目文档（正在跑）`。
- 已确认旧 runner 入口：
  - `h5_agent/runner/run_local_prototype.py`
  - `h5_agent/runner/run_local_h5.py`
- 本轮实际执行入口：`run_local_prototype.py`。
- 本轮未执行入口：`run_local_h5.py`。
- 旧项目依赖声明：`pyproject.toml`，requires-python `>=3.11`，依赖包括 `pillow`、`playwright`、`pydantic`、`fastapi`、`uvicorn`、`requests`。
- 本机实际执行 Python：`/Users/yiming/Documents/Codex/2026-06-17/h5-kv-h5-demo-dify-llm/.venv/bin/python`，版本 `Python 3.14.5`。
- dry-run 输入目录：`10_产品代码包/.h5-workbench/projects/current/legacy-task/dry-run/input/`。
- dry-run 输出目录：`10_产品代码包/.h5-workbench/projects/current/legacy-task/dry-run/output/`。
- dry-run report：`10_产品代码包/.h5-workbench/projects/current/legacy-task/dry-run/dry-run-report.md`。
- stdout / stderr / exitCode：`stdout.log`、`stderr.log`、`exit-code.txt`。
- 旧 runner 要求 KV 图；本轮在 dry-run input 中生成 `kv.png` 占位图，只用于验证 runner 可执行，不是正式素材。
- 本轮执行结果：exitCode `0`，状态 `dry_run_success`。
- 旧 runner 产物：
  - `01_blueprint.json`
  - `02_prototype.html`
  - `03_prototype_full.png`
  - `04_slices/`
  - `07_quality_report.md`
  - `run_summary.json`
- 当前 adapter 识别结果：`02_prototype.html` 已复制到稳定路径 `legacy-task/dry-run/prototype.dry-run.html`。
- FileManifest 新增：
  - `file_legacy_dry_run_report`：`dry-run-report.md`，status `dry_run_success`。
  - `file_prototype_dry_run`：`prototype.dry-run.html`，status `dry_run_success`。
- `GET /api/projects/current/files/file_legacy_dry_run_report` 可读取 report。
- `GET /api/projects/current/files/file_prototype_dry_run` 可读取 dry-run HTML。
- 安全验证：运行前后正式 `prototype.html` 和 `h5-artifact.json` 的 SHA-256 保持不变。
- 本轮没有接真实生图服务，没有运行 `run_local_h5.py`，没有把 dry-run 产物应用为正式原型。

## 旧 runner 原型 Review & Apply 状态

本轮已完成第十六步：把旧 runner 生成的 `prototype.dry-run.html` 从技术产物升级为可人工审稿、可应用、可放弃的候选原型。

- 新增 API：`POST /api/prototypes/dry-run/apply`。
- 新增 API：`POST /api/prototypes/dry-run/discard`。
- 候选原型路径：`10_产品代码包/.h5-workbench/projects/current/legacy-task/dry-run/prototype.dry-run.html`。
- 正式原型路径：`10_产品代码包/.h5-workbench/projects/current/prototype.html`。
- 应用备份路径：`10_产品代码包/.h5-workbench/projects/current/prototype.previous.html`。
- apply 行为：检查候选原型存在，备份上一版正式 `prototype.html`，再把 `prototype.dry-run.html` 应用为正式 `prototype.html`。
- apply 安全边界：不修改 `h5-artifact.json`，不自动启动长图生成，不运行 `run_local_h5.py`。
- discard 行为：只把 FileManifest 中的 `prototype.dry-run.html` 标记为 `discarded`，不删除文件，不回退或修改正式 `prototype.html`。
- FileManifest 当前状态：
  - `file_prototype`：`prototype.html`，status `legacy_applied`。
  - `file_prototype_previous`：`prototype.previous.html`，status `ready`。
  - `file_prototype_dry_run`：`prototype.dry-run.html`，本轮先验证 apply 时为 `applied`，随后验证 discard API 后标记为 `discarded`；正式 `prototype.html` 仍保持旧 runner 应用版本。
- hash 验证：
  - 当前正式 `prototype.html` 与 `prototype.dry-run.html` SHA-256 一致：`02fd14a8855d64680f9857d6a548a01ff50aa6f518b4a62edb0799a5c41a7da7`。
  - `prototype.previous.html` 保留旧正式稿 SHA-256：`855a1aecac54c45a4c915c3a3ba610853ca74aef11f5337aaa66b124b73670fd`。
  - 正式 `h5-artifact.json` SHA-256 保持不变：`00af5829ea39929065b3f0e8787c358d49c9c9b0500c64c78537943d08a8c2b5`。
- 前端：右侧「设计文件」tab 新增「旧 runner 候选原型」区域，提供查看候选原型、应用为正式原型、放弃候选原型；正式原型已由旧 runner 应用时，原型预览 tab 会显示来源「旧 runner 已应用」并加载正式 HTML。
- API 可读：`GET /api/projects/current/files/file_prototype_dry_run`、`GET /api/projects/current/files/file_prototype`、`GET /api/projects/current/files/file_prototype_previous` 均可返回内容。
- 本轮没有接真实生图服务，没有修改正式 `h5-artifact.json`，没有把 dry-run 产物伪装成真实长图链路。

## Home 接 legacy 原型候选生成状态

本轮已完成第十七步：Home 的「生成 H5 原型」按钮不再只是创建 mock 项目，而是优先通过 daemon 启动 `legacy_prototype_generation`。

- 新增 runType：`legacy_prototype_generation`。
- 新增 API：`POST /api/projects/create-from-brief`。
- 请求输入：`brief`、`templateId`、`designSystem`、`language`、`h5Type` 等 Home 配置。
- daemon 行为：创建/更新 current 项目，写入 `project.json`、`brief.md` 和初始化 `h5-artifact.json`，随后启动旧 runner 生成候选原型。
- 旧 runner 实际入口：`/Users/yiming/Documents/Codex/2026-06-17/h5-kv-h5-demo-dify-llm/h5_agent/runner/run_local_prototype.py`。
- 旧 runner 执行目录：`/Users/yiming/Documents/Codex/2026-06-17/h5-kv-h5-demo-dify-llm`。
- 候选输入目录：`10_产品代码包/.h5-workbench/projects/current/legacy-task/candidate/input/`。
- 候选输出目录：`10_产品代码包/.h5-workbench/projects/current/legacy-task/candidate/`。
- 本轮候选产物：
  - `prototype.candidate.html`
  - `prototype.candidate.png`
  - `legacy-blueprint.candidate.json`
  - `legacy-quality-report.md`
  - `legacy-candidate-report.md`
  - `stdout.log`
  - `stderr.log`
  - `exit-code.txt`
- FileManifest 当前状态：
  - `file_prototype_candidate`：`prototype.candidate.html`，status `applied`。
  - `file_prototype_candidate_png`：`prototype.candidate.png`，status `legacy_candidate`。
  - `file_legacy_blueprint_candidate`：`legacy-blueprint.candidate.json`，status `legacy_candidate`。
  - `file_legacy_quality_report`：`legacy-quality-report.md`，status `legacy_candidate`。
  - `file_legacy_candidate_report`：`legacy-candidate-report.md`，status `ready_for_review`。
  - `file_prototype`：`prototype.html`，status `legacy_applied`。
  - `file_prototype_previous`：`prototype.previous.html`，status `ready`。
- 安全验证：
  - Home 触发候选生成后，正式 `prototype.html` 未自动覆盖。
  - 手动 apply 后，`prototype.html` 与 `prototype.candidate.html` SHA-256 一致：`02fd14a8855d64680f9857d6a548a01ff50aa6f518b4a62edb0799a5c41a7da7`。
  - apply 前的正式 `prototype.html` 已备份为 `prototype.previous.html`，SHA-256：`855a1aecac54c45a4c915c3a3ba610853ca74aef11f5337aaa66b124b73670fd`。
  - apply 前后正式 `h5-artifact.json` SHA-256 保持不变：`788cfabe890a0d05615cd3f251d75d212b7487601b04dc14023bd95f903c06d1`。
  - 未运行 `run_local_h5.py`，未接真实生图，未自动启动长图生成。
- 前端行为：Home 生成后进入 Project Workspace，左侧 Agent 面板通过 SSE 显示旧 runner 生成过程；右侧「设计文件」出现旧 runner 候选原型 Review 区，可查看候选原型并手动应用。

## 下一步建议

1. 在确认正式 current 不再有待处理 run 后，受控重启 7458 daemon，使本轮双哈希 Review、managed asset 与 production-integrity API 在正式进程生效；重启前后继续核对 current/snapshots 哈希。
2. 用当前 `ready_for_review` 候选做一次真实人工审核：重新查看取得 HTML + blueprint 双哈希，再应用、确认；旧 KV 必须通过真实字节校验并完成 managed metadata 迁移。
3. 给候选审核区补充可见的双哈希摘要和 production-integrity 详情入口；当前协议和请求链路已完成，但 UI 只保留必要提示。
4. 暂缓 `run_local_h5.py`、真实生图和真实 ZIP 导出；先观察完整性门槛在真实设计任务中的误报与迁移体验。

## 注意事项

- 不要推翻现有两级工作台结构。
- 不要把默认页改成单纯 H5 编辑器。
- 不要删除或移动旧项目文件。
- 不要把 `05_Agent流程/` 当作新项目入口。
- 不要把 Open Design 全量照搬，只参考工作台结构和交互模式。
- 文档和开发日志使用中文。
