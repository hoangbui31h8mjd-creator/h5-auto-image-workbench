# 2026-07-10 Guardian 检查与前端工作台状态更新

## 本次修改背景

用户要求使用 `$h5-workbench-guardian` 检查最近一轮前端改动是否跑偏，并更新开发日志。

最近一轮前端改动的目标是：在不推翻现有「一级 Home + 二级 Project Workspace」结构的前提下，补齐左侧 Agent 执行面板、生成长图流程、素材/KV 槽位、短项目标题和右侧设计文件产物关系。

## 本次目标

- 按 guardian 规则检查产品方向是否仍对齐 H5 Agent 工作台。
- 确认没有变成普通后台、单纯编辑器、普通图片生成器或 Open Design 完整复刻。
- 确认前端仍是 mock 第一版，不接真实 runner、不接账号、不做团队协作。
- 创建并更新 `11_开发日志/00_当前开发状态.md`。
- 写入本次日期日志，方便下一个 Codex 窗口接手。

## 检查范围

- `00_新工作台主入口.md`
- `AGENTS.md`
- `$h5-workbench-guardian` 的 `references/log-templates.md`
- `$h5-workbench-guardian` 的 `references/h5-workbench-rules.md`
- `10_产品代码包/apps/web/src/App.tsx`
- `10_产品代码包/apps/web/src/components/AgentRunPanel.tsx`
- `10_产品代码包/apps/web/src/components/DesignFilesView.tsx`
- `10_产品代码包/apps/web/src/components/LongImageView.tsx`
- `10_产品代码包/apps/web/src/components/AssetSlotsView.tsx`

## Guardian 检查结论

本次改动没有跑偏，方向是正确的。

对照 guardian 规则：

- Home page 仍通过中央 Composer 启动 H5 任务。
- Project page 保留顶部全局项目 tab 和右侧二级 tab。
- 左侧面板已从普通日志框升级为产品化 Agent 执行面板。
- raw CLI log 只在调试模式折叠区出现，没有默认变成黑框终端。
- 右侧工作区覆盖设计文件、原型预览、生成长图、素材/KV。
- 原型编辑和 Inspector 被放在「原型预览」tab 内，没有成为项目页唯一主体。
- 生成长图 tab 已有六步 staged pipeline，即使当前仍是 mock。
- 素材/KV 已以槽位形式表达，并映射到 `h5-artifact.json` 的 `imageSlots`。
- 项目短标题已修正为「游戏活动 H5」这类短名，完整需求放在左侧任务摘要。

## 修改文件

- `11_开发日志/00_当前开发状态.md`：新增当前项目状态、运行方式、已完成能力、尚未接入能力、guardian 检查结论和下一步建议。
- `11_开发日志/2026-07-10_Guardian检查与前端工作台状态更新.md`：新增本次检查日志。

## 交互现状

- 默认打开 `http://127.0.0.1:5178/` 进入 Home。
- 用户输入需求并点击「生成 H5 原型」后进入 Project Workspace。
- 顶部项目 tab 显示短标题，例如「游戏活动 H5」。
- 左侧 Agent 面板显示任务摘要、Checklist、输出、产物、活动日志、下一步建议和 raw CLI log。
- 右侧默认显示设计文件生产链路。
- 点击「生成长图」可运行 mock 六步流程。
- 点击「素材/KV」可查看 6 个素材槽位，并进行 mock 上传、替换、清除。

## 验收结果

- 页面是否能启动：可以。
- 本地地址：`http://127.0.0.1:5178/`
- 服务检查：`curl -I http://127.0.0.1:5178/` 返回 `HTTP/1.1 200 OK`。
- 测试命令：`pnpm typecheck` 通过。
- 构建命令：`pnpm --filter @h5-workbench/web build` 通过。
- 手动/浏览器验证：上一轮已验证 Home、项目页、短标题、7 个设计文件、长图 mock 流程、素材槽位和刷新后无新 console error。

## 尚未解决

- 还没有把 `AgentEvent` 抽到 `packages/agent-contract`。
- 还没有接 daemon 的事件流。
- 还没有接 Codex CLI / Claude CLI。
- 还没有接旧 Python runner 的切片和生图能力。
- 真实素材上传、真实导出和文件落盘仍未实现。

## 下一步建议

1. 先定义 `packages/agent-contract` 的事件协议和 run 状态协议。
2. 再在 `apps/daemon` 做 mock event stream，让前端从本地 state 迁移到 daemon 事件源。
3. 然后才接旧 Python runner：从切片计划、分段生图、拼接长图三个能力点逐步抽取，不要搬旧项目整体。

## 给下一个 Codex 窗口的提示

先读：

- `00_新工作台主入口.md`
- `AGENTS.md`
- `11_开发日志/00_当前开发状态.md`
- `11_开发日志/2026-07-10_Guardian检查与前端工作台状态更新.md`

然后继续：

- 不要推翻现有框架。
- 不要把项目页改成普通 SaaS 后台。
- 不要把原型编辑器作为默认唯一主体。
- 不要接真实 runner 之前就复活旧 Dify / ComfyUI 链路。
- 下一步优先做 `agent-contract` 和 daemon mock event stream。
